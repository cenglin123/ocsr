# Converge 合同：OCSR 看门狗与 Session 恢复

状态：已执行（2026-09-11 落地；合同条款与主计划一致实现，验收通过）

独立复核结论：可执行，无阻断问题。归档证据：`.converge/done/20260911-ocsr-watchdog-session-recovery/evidence/ocsr-final-review.md`。

交接要求：执行 agent 必须保持下列合同与主计划一致；reviewer 提出的非阻断改进可以在实现阶段一并处理，但不得把 watcher 改成自动重派或绕过 reserve/settle 账本。

## 产物

项目代码、测试、`SKILL.md`/专题文档及遥测 schema 的最小一致修复。

## 必须满足

- 超时先检查、再裁决；存在进展时可有限续期，存在不确定性时不并发重派。
- session ID 只能来自明确运行时证据；续接必须在旧执行停止后进行。
- 总时限、续期次数、预算和尝试数均有硬上限。
- 文件回收区分“已出现”与“已完成/已验收”。
- 现有安全边界与退出码契约保持诚实。

## 不接受

- 以 SQLite 最新 session 猜测并自动恢复。
- 每次超时无限顺延 deadline。
- 仅凭日志字节数判定进展或完成。
- 先杀进程再让 agent 检查。
- 以 session 续接掩盖副作用未核对或预算未记账。

## 验证

运行项目既有验证命令，并新增针对状态机边界的确定性测试。
